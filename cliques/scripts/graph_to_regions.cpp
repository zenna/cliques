#include <iostream>
#include <vector>

#include <lemon/list_graph.h>
#include <lemon/smart_graph.h>
#include <lemon/concepts/graph_components.h>
#include <lemon/concepts/graph.h>

#include <cliques/helpers/helpers.h>
#include <cliques/quality_functions/stability.h>
#include <cliques/algorithms/louvain.h>
#include <cliques/structures/disjointset.h>

int main() {

	// Load graph from edgelist -> G

	// Create / load hierarchical stability graph
		// optimise louvain over range
		   // if sufficiently different from before
			   // draw graph with this as partition


	// Create HSG with map relating node <-> list of nodes in original graph
	for level in levels
		draw(original_graph, colourmap, positions);


};
  //Graph must be in correct format
//Perform stability optimisation on it
  //Call matlab for this or implement my own method
//Perform a random walk on it
  // Call my own code
//Perform a correlation complexity measure
  //  Implement in C++
//Find relevant robust partitions
//Create hierarchihcal stabaility graph
//Map graph to brain regions
