 
To install the Matlab interface for Clique, copy the files 
from the folder "matlab" into a new folder where you want 
the matlab toolbox Clique to be installed.

Then, simply run from Matlab:

>> Install_Stability('path-to-clique-cxx-folder')

You can then run stability as in the other package.